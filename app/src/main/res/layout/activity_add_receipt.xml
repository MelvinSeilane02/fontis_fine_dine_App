<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@color/white"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout android:orientation="vertical"
        android:layout_width="match_parent" android:layout_height="wrap_content">

        <TextView
            android:text="Add Receipt"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/ThemeGreen"/>

        <!-- Order selector: simplest: free-text orderId or choose from dropdown -->
        <EditText
            android:id="@+id/etOrderId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Order ID (or leave blank if unknown)"
            android:layout_marginTop="12dp"/>

        <EditText
            android:id="@+id/etNotes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Notes (e.g., received via WhatsApp on dd/mm)"
            android:layout_marginTop="8dp"/>

        <ImageView
            android:id="@+id/ivPreview"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="12dp"
            android:scaleType="centerCrop"
            android:src="@drawable/receipt_long_icon_black"/>

        <Button
            android:id="@+id/btnPickImage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Choose Image"
            android:layout_marginTop="12dp"
            android:backgroundTint="@color/ThemeYellow"
            android:textColor="@color/black"/>

        <Button
            android:id="@+id/btnUpload"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Upload Receipt"
            android:layout_marginTop="12dp"
            android:backgroundTint="@color/ThemeGreen"
            android:textColor="@color/white"/>

    </LinearLayout>
</ScrollView>
